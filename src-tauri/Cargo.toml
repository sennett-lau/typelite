[package]
name = "typelite"
version = "0.1.0"
description = "AI-powered speech-to-text and text polishing desktop app"
authors = ["Sennett Lau"]
license = "MIT"
edition = "2021"
rust-version = "1.85"

[lib]
name = "typelite_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
devtools = ["tauri/devtools"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "=2.11.2", features = ["tray-icon", "macos-private-api"] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-store = "2"
tauri-plugin-clipboard-manager = "2"
tauri-plugin-autostart = "2"
tauri-plugin-opener = "2"
tauri-plugin-single-instance = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
cpal = "0.15"
enigo = "0.2"
tokio = { version = "1", features = ["rt", "macros", "sync", "time", "test-util"] }
reqwest = { version = "0.12", features = ["json", "stream", "multipart"] }
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
futures-util = "0.3"
async-trait = "0.1"
arboard = "3"
http = "1"
rusqlite = { version = "0.31", features = ["bundled"] }
csv = "1.3"
unicode-normalization = "0.1"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = "0.4"
url = "2"
# Plan `qwen-cloud-speech`: Qwen Cloud takes the recording as a base64 data URI inside JSON.
base64 = "0.22"
tauri-plugin-process = "2"
# Plan `quick-speech-setup`: in-process whisper.cpp for the "Built-in (this Mac)" speech provider
# (Unlicense). Builds whisper.cpp from source with cmake; logs go through `tracing`.
whisper-rs = { version = "0.16", features = ["tracing_backend"] }
sha2 = "0.10"
fs4 = { version = "0.13", features = ["sync"] }
# Plan `preset-sharing`: native save and open dialogs for preset Export and Import, called from Rust.
tauri-plugin-dialog = "2"

[target.'cfg(target_os = "windows")'.dependencies]
keyring = { version = "3.6.3", default-features = false, features = ["windows-native"] }
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_System_LibraryLoader",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_WindowsAndMessaging",
    "Win32_System_Threading",
] }

[target.'cfg(target_os = "macos")'.dependencies]
keyring = { version = "3.6.3", default-features = false, features = ["apple-native"] }
# Objective-C runtime calls for the Microphone permission (AVCaptureDevice). Already pulled in
# by Tauri, so these add no new crates.
objc2 = "0.6"
block2 = "0.6"
# Already in the tree through cpal; used directly to mute the output device while recording.
coreaudio-sys = { version = "0.2", default-features = false, features = ["core_audio"] }
# GPU (Metal) for the built-in speech provider.
whisper-rs = { version = "0.16", features = ["tracing_backend", "metal"] }
# Already in the tree; `sysctlbyname` reads the chip and memory for the built-in model check.
libc = "0.2"

[target.'cfg(target_os = "linux")'.dependencies]
keyring = { version = "3.6.3", default-features = false, features = ["linux-native-sync-persistent", "crypto-rust"] }
libloading = "0.8"

[dev-dependencies]
plist = "1"
tauri = { version = "=2.11.2", features = ["test"] }

[profile.release]
codegen-units = 1
opt-level = "s"
lto = "thin"
strip = true

# whisper.cpp (built by cmake from whisper-rs-sys) follows the package's opt-level: 0 would
# make a Debug build and "s" a MinSizeRel build, both much slower at transcribing. Build it
# fully optimised in every profile.
[profile.dev.package.whisper-rs-sys]
opt-level = 3

[profile.release.package.whisper-rs-sys]
opt-level = 3
